name: API Manager
on:
  push:
    paths:
      - 'resources/ApiManager/**'
      - '.github/workflows/resource-api-manager.yml'        # When Pipeline File Changes

permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        projects: [
            'Assimalign.Cohesion.ApiManager',
            'Assimalign.Cohesion.ApiManager.ApplicationModel',
            'Assimalign.Cohesion.ApiManager.Hosting'
        ]
    steps:
      - uses: actions/checkout@v4
        name: "Checkout: ${{ matrix.projects }}"

      - uses: ./.github/actions/build
        name: "Running Build & Tests: ${{ matrix.projects }}"
        with:
          area: 'resources'
          category: 'ApiManager'
          project: ${{ matrix.projects }}
