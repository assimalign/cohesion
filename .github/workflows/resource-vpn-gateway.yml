name: VPN Gateway
on:
  push:
    paths:
      - 'resources/VpnGateway/**'
      - '.github/workflows/resource-vpn-gateway.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        projects: [
            'Assimalign.Cohesion.VpnGateway',
            'Assimalign.Cohesion.VpnGateway.Hosting'
        ]
    steps:
      - uses: actions/checkout@v4
        name: "Checkout: ${{ matrix.projects }}"

      - uses: ./.github/actions/build
        name: "Running Build & Tests: ${{ matrix.projects }}"
        with:
          area: 'resources'
          category: 'VpnGateway'
          project: ${{ matrix.projects }}
