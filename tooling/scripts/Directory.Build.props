<Project>
	<!--
	    Chains to the tooling area props (not the repo root) so this subtree inherits
	    $(TargetFrameworkForTooling); importing the root directly left TargetFramework
	    empty and broke the build (NETSDK1013).
	-->
	<Import Project="..\Directory.Build.props" />
</Project>