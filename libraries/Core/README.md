# Core libraries

The Core area contains dependency-light Layer 1 primitives shared across Cohesion. Its base
package, `Assimalign.Cohesion.Core`, owns the frozen gateway-to-resource environment contract,
environment-name resolution, and general-purpose value types without taking hosting, dependency
injection, configuration, or logging dependencies.
