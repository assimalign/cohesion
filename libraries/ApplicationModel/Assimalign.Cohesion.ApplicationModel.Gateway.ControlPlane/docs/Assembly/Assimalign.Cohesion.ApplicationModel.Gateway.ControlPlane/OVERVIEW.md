# Assimalign.Cohesion.ApplicationModel.Gateway.ControlPlane

Provides the NativeAOT-safe gateway HTTP server, authenticated export client, Local discovery
metadata writer, and area command-dispatch seam. The public namespace is exactly the assembly
name; all listener, routing, JWT, JSON, and command-observation implementations are internal.
