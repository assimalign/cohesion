# LogSpace SinkHost

Executable used by LocalGateway acceptance tests for authenticated OTLP/HTTP JSON ingest, HTTPS application trust, mounted segments and authenticated query read-back. It uses the ambient resource contract and has no standalone settings.
