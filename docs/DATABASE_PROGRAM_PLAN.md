# Database Program Plan (Data Platform, L03.02)

**Status:** active · **Created:** 2026-07-06 · **Owner:** Chase Crawford · **Scope:** the multi-model OLTP database platform — `resources/Database/*`, the `Assimalign.Cohesion.Sdk.Database` build tooling, and the `App.Database` framework family. GitHub epics **#31 / #4 / #5 / #50 / #57 / #856** (WBS `L03.02.*`).

> **Why this file exists.** This program spans ~77 GitHub work items across 6 epics and will be implemented by many separate AI coding sessions. No single session holds the whole picture in context. This document is the **durable sequencing index**: it records what depends on what, what is safe to do in parallel, and the protocol each session follows. GitHub issues hold the *what* and *acceptance criteria*; this file holds the *when* and *in-what-order*. The architecture itself lives in `resources/Database/DESIGN.md` — read that first; this file assumes it.

This file is temporary scaffolding for the duration of the program. When the five engines reach MVP and this backlog drains, fold anything durable into the relevant `docs/DESIGN.md` files and delete this doc.

---

## 1. How to run this across multiple sessions (read first)

The safe unit of work is **one GitHub issue = one session = one branch = one PR**.

**The session protocol (every session follows this):**

1. **Pick an issue that is unblocked.** An issue is workable only if every entry in its *Blocked by* column (§4) is merged. Never start a blocked issue — its prerequisites define types/seams you would otherwise invent and later fight.
2. **Read four things before coding:** (a) the issue body and acceptance criteria; (b) this plan's row for the issue and the lane guardrails in §3; (c) `resources/Database/DESIGN.md` (the architecture) plus the touched project's `docs/DESIGN.md`; (d) the repo coding rules (`.claude/rules/`, auto-loaded in Claude sessions).
3. **Branch:** `feature/<wbs>-<slug>` naming the issue's WBS (e.g. `feature/L03.02.01.03-mvcc-substrate`). The `cohesion-work-items` skill infers scope-creep placement from this branch.
4. **Implement to the acceptance criteria.** File out-of-scope discoveries with the `cohesion-work-items` skill (don't expand the current issue) and call them out in the PR description so the orchestrator can slot them into §4.
5. **Open a PR** with one `Closes #NNNN` per line (use `New-CohesionWorkItem.ps1 -EmitClosesBlock` from the same worktree).
6. **Do not edit this plan file.** The orchestrator reconciles §5 from merged PRs — that avoids shared-doc merge conflicts when many sessions run in parallel.

**Golden rule for parallelism:** issues in different **lanes** (§3) at the same **stage** (§2) can run concurrently with no coordination. Two sessions in the *same* lane touching the same project must be serialized — check §5 and open PRs for an in-flight sibling before starting.

**Prompt template for a session:**

```
Work GitHub issue #NNNN in assimalign/cohesion.

Before coding, read docs/DATABASE_PROGRAM_PLAN.md — follow the Session Protocol in §1,
confirm the issue is unblocked per §4, and honor the lane guardrails in §3. Read
resources/Database/DESIGN.md for the architecture and follow the repo coding
rules (.claude/rules). Branch, implement to the issue's acceptance criteria, and open a PR that closes it.

If the issue is blocked per §4, stop and tell me which prerequisite is outstanding.
```

---

## 2. Stages (dependency gates)

A stage is a gate, not a calendar. Everything in a stage may proceed once the prior-stage items it depends on are merged. (Stages are finer-grained than the GitHub `Wave` field; treat this document as the authority on order.)

| Stage | Theme | Gate to enter |
|---|---|---|
| **0 — Ground truth** | Area scaffold, design docs, structural fixes. Landed by the scaffold PR (#855). | done |
| **1 — Kernel** | Storage repair + durability, the ACID substrate, type system, execution pipeline, wire protocol. Everything downstream imports from here. | none |
| **2 — Kernel build-out + languages** | B+Tree indexing on the kernel; per-model language work (always parallel-safe); SQL catalog. | its Stage-1 prerequisites |
| **3 — Model engines** | Each model's storage layout, catalog, and engine lifecycle composed from the kernel. | its Stage-1/2 prerequisites |
| **4 — Service surface** | Server/client end-to-end, per-model clients, security, hosting/governance/health, orchestration manifest. | a working engine + protocol |
| **5 — Tooling & hardening** | Database projects, migration engine, conformance corpora, crash suites at scale. Replication is **post-MVP** and intentionally last. | catalog + language stability |

**The three most load-bearing edges in the program:**
- **#158 (page/buffer management) and #160 (journal ordering + recovery)** gate the entire kernel — land them early and review them hardest.
- **#850 (MVCC/transactions)** gates every engine's write path and #851 (indexing).
- **#852 (protocol/server/client core)** gates every per-model client and the hosted end-to-end path.

---

## 3. Lanes (what can run in parallel) + per-lane guardrails

| Lane | Area | Projects | Guardrail (the thing sessions get wrong) |
|---|---|---|---|
| **K — Storage kernel** | pages, WAL, recovery, backup | `Database.Storage` | The journal is the *only* durability mechanism; no side files. Page CRC on every read path. Keep contracts model-agnostic — model layouts live in `{Model}.Storage`, never here. |
| **T — ACID substrate** | transactions, locks, versions, types, indexing | `Database.Transactions`, `Database.Types`, `Database.Indexing` | `TransactionSequence` (ordering) vs `TransactionId` (identity) — don't conflate. Index mutations always take `ITransactionContext`. Type encodings must be order-preserving under raw byte compare. |
| **X — Execution** | sessions, pipeline, plans | `Database.Execution`, root `Database` | Model-agnostic: no SQL/document semantics in shared operators. `QueryRequest`/`QueryResult` families stay abstract; models subclass. |
| **P — Protocol & clients** | wire protocol, server runtime, client core | `Database.Protocol`, the per-model servers (`Database.Sql`'s `SqlDatabaseServer`; the machinery folded into the model package 2026-07-14 — the root contract is the only area-wide server requirement), `Database.Client` | Framing is model-agnostic; model semantics live in payloads. Every limit (payload size, sessions, timeouts) is DoS-critical — enforce at parse/accept. Test over `Connections.InMemory`, never live sockets. |
| **LS / LD / LG — Languages** | per-model parsers | `Sql.Language`, `Documents.Language`, `Graph.Language` | Always parallel-safe (leaf dependencies). Declared-dialect contract before deep implementation; conformance corpora guard the AST. Graph language work gates on the #193 standard decision. |
| **MS / MD / MK / MB / MG — Model engines** | engine + model storage + catalog | `Database.{Sql,Documents,KeyValuePair,Blob,Graph}` + their `.Storage`/`.Catalog` | Compose the kernel — never re-implement paging/WAL/locking locally. Every write path goes through `ITransactionContext`. Engines never depend on other engines. |
| **H — Hosting & ops** | host, governance, health, orchestration | `Database.Hosting`, `Database.Governance`, `Database.ApplicationModel` | WAL flush / page writer = `DedicatedThreadService`; endpoint = `BackgroundService` (Hosting DESIGN.md menu). `*.Hosting` is the ONLY DI/Config/Logging seam. The manifest project never references the runtime. |
| **D — Developer tooling** | SDK build tasks, migrations | `sdks/Assimalign.Cohesion.Sdk.Database` | Build-time work in MSBuild tasks; transactional apply in the engine catalog. Static per-model imports — no runtime plugin loading. |

Cross-cutting rules (all lanes): file-scoped namespaces; `CohesionProjectReference`/`CohesionPackageReference`; **no `Microsoft.Extensions.*`**; `IsAotCompatible=true`, no reflection; interface-first with internal impls; XML docs on public APIs; Shouldly tests co-located; update the touched project's `docs/DESIGN.md` in the same change. The rules in `.claude/rules/` are canonical.

---

## 4. The work items (with blockers)

"Blocked by" lists *hard* prerequisites (types/seams that must exist first). Soft coordination is noted inline. Feature-level issues (4-segment WBS) appear where their task children (5-segment) are the real work units — sessions work the tasks; features close when their children do.

### Stage 1 — Kernel

| Issue | Lane | Title | Blocked by |
|---|---|---|---|
| #157 | K | Fix missing storage model types + align storage abstractions | — (scaffold PR restored `StorageModel`; the *alignment* remains) |
| #158 | K | Page allocation, pinning, eviction, buffer reuse | #157 · **(gates the kernel)** |
| #159 | K | Tests: corruption detection, concurrency, lifecycle edges | #158 |
| #160 | K | Journal write ordering + recovery replay rules | #157 · **(gates durability)** |
| #161 | K | Backup/restore flows + version compatibility | #160 |
| #162 | K | Crash/restart/restore suites proving durability | #160 |
| #911 | K | Per-object record chains: table scans stop decoding the whole database (landed — fable5 sql-performance PR, see §5) | #162, the MVCC integration (#908) |
| **#850** | **T** | **Transactions, MVCC, concurrency control (ACID substrate)** | #160 (durable commit path; contract + in-memory work can start immediately) · **(gates all engine write paths)** |
| #854 | T | Shared type system and collation foundation | — |
| #163 | X | Common execution context and pipeline contracts | — |
| #164 | X | Transaction boundaries, commit/rollback semantics, error propagation | #850, #163 |
| #165 | X | Tests: cancellation, failures, multi-stage execution | #163 |
| **#852** | **P** | **Wire protocol, server front-end, shared client core** | #163 (execution dispatch; framing/handshake work can start immediately) · **(gates all clients)** |

### Stage 2 — Kernel build-out + languages (all language items are parallel-safe from day one)

| Issue | Lane | Title | Blocked by |
|---|---|---|---|
| **#851** | **T** | **Shared index infrastructure: B+Tree, keys, cursors** | #158, #160, #850, #854 |
| #169–#171 | LS | SQL parser coverage, AST/diagnostics stability, conformance corpora (feature #39) | — |
| #172–#174 | LS | Declared SQL dialect contract, type/builtin coverage, coercion tests (feature #40) | #854 (type identities) |
| #181–#183 | LD | OQL grammar/AST contract, diagnostics, conformance corpora (feature #45) | — |
| #193 | LG | **Decision:** select the graph query standard (ISO GQL recommended) | — · **(gates all Graph language/engine work)** |
| #194–#195 | LG | Graph grammar/AST/diagnostics + conformance plan (feature #52) | #193 |
| #175 | MS | Relational catalog objects + metadata persistence | #158, #164 |
| #177 | MS | Relational security: principals, permissions, schema changes | #175 (shared contracts in `Database.Security` co-evolve) |

### Stage 3 — Model engines

| Issue | Lane | Title | Blocked by |
|---|---|---|---|
| #178 | MS | Logical/physical planning from SQL AST + catalog | #170, #175, #851 |
| #179 | MS | Execute relational plans against shared storage | #178, #164 |
| #912 | MS | CREATE/DROP INDEX + MVCC index maintenance on the SQL write path (landed — fable5 sql-performance PR, see §5) | #851, #179, #908/#909, #911 |
| #913 | MS | Planner adopts secondary indexes: equality and range seeks (landed — fable5 sql-performance PR, see §5) | #912 |
| #914 | MS | Index-adoption E2E over the wire + program documentation (landed — fable5 sql-performance PR, see §5) | #912, #913, #904 (the E2E harness) |
| #184–#186 | MD | Document query planning, projection/aggregation/mutation, tests (feature #46) | #181, #163 |
| #187 | MD | Document persistence + versioned metadata | #158, #160, #850 |
| #188 | MD | Document index definitions + maintenance | #851, #187 |
| #189 | MD | Document serialization rules (objects/arrays/scalars) | #854 |
| #205 | MK | KV storage + metadata handling | #851, #850 |
| #208–#210 | MK | Cache command surface, eviction/expiry, coherence tests (feature #60) | **post-MVP** (behind #205) |
| #211 | MB | Blob persistence, chunking, metadata catalog, lifecycle | #158, #160, #850 |
| #196–#198 | MG | Graph schema/traversal contracts, diagnostics, tests (feature #53) | #193 |
| #199 | MG | Durable graph storage + adjacency layout | #158, #850, #851 |
| #200 | MG | Graph catalog: schema and object discovery | #199 |

### Stage 4 — Service surface, hosting, orchestration

| Issue | Lane | Title | Blocked by |
|---|---|---|---|
| #166 | H | Hosting adapters + lifecycle alignment for database workloads | #163; engines start composing as they land |
| #167 | H | Quotas, tenancy/isolation boundaries, audit events | #166 |
| #168 | H | Health, readiness, diagnostics surfaces | #166 |
| **#853** | **H** | **ApplicationModel manifest + AddDatabase integration test** | #166 |
| #180 | MS | SQL client: commands, result sets, errors, telemetry | #852, #179 |
| #190 | MD | Document client APIs: queries, mutations, diagnostics | #852, #184 |
| #207 | MK | KV client APIs, result models, diagnostics | #852, #205 |
| #214 | MB | Blob client: upload/download/metadata (streaming) | #852, #211 |
| #202 | MG | Graph client APIs and error surfaces | #852, #199 |
| #176 | MS | Migration workflows, compatibility checks, rollback (catalog side) | #175 |
| #191, #201, #206, #212 | M* | Per-model security rules and protected-operation checks | #177 (shared pattern), each model's catalog |
| **#862** | **H** | **Embedded consumption: engine self-sufficiency + reference resource adoption** | #850; at least one engine with working lifecycle (#187 or #205 recommended first) |
| **#907** | **T/MS** | **MVCC integration 1/4: bind engine sessions to the transaction manager + end-to-end isolation levels** (landed — fable5 MVCC batch, see §5) | #850, #179 (both landed) |
| **#908** | **T/MS** | **MVCC integration 2/4: row-level version stamping + snapshot-visible scans** (landed — fable5 MVCC batch, see §5) | #907 |
| **#909** | **T/MS** | **MVCC integration 3/4: row-grain write conflicts via the lock manager + deadlock surfacing** (landed — fable5 MVCC batch, see §5) | #907 |
| **#910** | **T/MS** | **MVCC integration 4/4: version-purge worker activation (PurgeWriterAsync + safe prune bound)** (landed — fable5 MVCC batch, see §5) | #908 |
| #213 | MB | Tests: large-object persistence, metadata, access control | #211, #212 |

### Stage 5 — Tooling & post-MVP

| Issue | Lane | Title | Blocked by |
|---|---|---|---|
| **#859** | **D** | **Database project system: declarative schema compile** | #170 (stable AST), #172 (declared dialect) |
| **#857** | **D** | **Migration engine: diff, script gen, transactional apply** | #859, #176 |
| **#858** | **D** | **Model-specific build-tool loading + per-model targets** | #859 |
| **#861** | **K** | **Encryption at rest (pages/WAL/backups) via the Security.DataProtection key ring** | #158, #160, #162 (crash suites must exist to re-run under encryption) |
| #192, #203–#204, #215–#216 | M* | Per-model replication topology/consistency/tests | **post-MVP** — sequenced after every engine reaches MVP; shared log-shipping seam over the WAL comes first (new item to file when scheduled) |

### MVP definition (what "done" means for the first cut)

Every model: create/open/drop databases; CRUD in its native shape; ACID (crash-recovery suite passes); reachable through the wire protocol with a working client. SQL additionally: declared dialect subset with conformance corpus, catalog + migrations. Explicitly **not** MVP: Cache model, replication, cost-based planning, Serializable isolation, cross-model queries.

---

## 5. Progress Log (orchestrator-reconciled from merged PRs)

Sessions do not edit this table; the orchestrator reconciles it from merged PRs.

| Date | Issue | PR | Notes |
|---|---|---|---|
| 2026-07-14 | #911, #912, #913, #914 | (fable5 sql-performance PR) | **The SQL performance keystone: per-object record chains, secondary indexes end-to-end, planner seek adoption** (four commits in dependency order). **#911 chains:** data pages carry an 8-byte owner tag (reserved header bytes @64); per-owner page directory rebuilt from the open-time header scan (headers are the truth — the FSM precedent), owner-scoped inserts/iteration (`IStorage.GetUnitIterator(ownerId)`/`GetOwnerPages`), transactional chain release (`FreeOwnerPages` — FSM/directory effects defer to COMMIT so an in-flight release can never be reallocated). SQL keys chains by table object id: table scans are O(table) not O(database); DROP TABLE releases its chain for reuse (O(pages-of-table), not O(1) — full-page-image WAL prices transactional frees per page; the O(1) direction = the reserved bitmap-FSM page type, recorded in Storage DESIGN). Record-space format v3, in-place crash-window-idempotent v2→v3 relocation at open (stamps verbatim; dropped-table residue discarded). **#912 index DDL + maintenance:** CREATE [UNIQUE] INDEX / DROP INDEX (dialect matrix + corpus updated together); catalog kind-5 `SqlCatalogIndex` descriptions persisted ATOMICALLY with the physical registrations (one self-commit — description and tree must never tear); DDL-blocking build under the table's X lock in a durably committed gated bracket, walking EVERY stored version with original stamps (older snapshots read exactly what the scan shows); DML maintenance mirrors the row-version discipline (writer stamps on insert, deleter tombstones on delete, tombstone+insert on update); UNIQUE = the B+Tree latest-state-under-hashed-key-lock discipline, violations translated to `DatabaseException` at the model boundary; **lock-ordering rule recorded** (IX table → row X sorted by location → unique key locks sorted by `IndexKey.Hash`, all phase-one, so nothing ever waits inside the apply gate); logical rollback undoes index stamps via the version-store ledger (new Indexing undo surfaces `EraseAsync`/`ClearDeleterAsync`); open-time recovery scrubs unproven writers per tree (`IIndexManager.PurgeWritersAsync`); registrations re-export at DDL/checkpoint/disposal with changed-set check (root drift). Index pages ride the same database file set (the transactional page surface) — confirmed + documented. **#913 planner seeks:** the SELECT plan gains the closed `SqlAccessPath` family (scan \| seek: equality prefix + optional range bounds + full-WHERE residual — wrong choices can cost performance, never correctness); longest-equality-prefix selection (ties: range→unique→name), range sargability restricted to types whose evaluator order provably equals codec byte order (strings equality-only — the #854 astral-plane lesson); executor drives cursors through the STATEMENT snapshot (`IIndex.OpenCursor(snapshot, …)`) + re-checks row stamps (a seek never surfaces what the scan would hide); sub-linear proof = per-statement `SqlStatementMetrics` (seek examines 1 record where the scan examines 2000+). **#914 E2E:** TCP + SqlClient over the executable's composition — DDL → wire-driven root-splitting load → indexed reads under concurrent DML on separate connections + uncommitted-writer invisibility → restart → recovered index serves the same truth, keeps enforcing UNIQUE, and fresh CREATE INDEX builds over recovered rows. Bonus fix: the version store's stamp probe tolerates slots reverted out of existence by bracket rollback (latent crash on rollback-after-failed-statement, surfaced by unique-violation tests). Suites at tip: Storage 71, Sql 100, Sql.Catalog 10, Sql.Language 166, Indexing 18, Sql.Storage 14, Transactions 30, Application E2E 18 (incl. TCP + real-process gateway). |
| 2026-07-14 | server-placement finalization (owner-approved) | (fable5 hardening PR, finalization) | **`Database.Server` folded into `Database.Sql`; the project deleted — the third and settled placement of the server machinery** (Hosting fold 2026-07-12 → shared base 2026-07-13 → Sql-internal 2026-07-14). The shared guided base was premature abstraction from n=1: one model server existed, and future model servers (Blob streaming, KV binary command paths) will diverge from the SQL-shaped execute pump. The machinery (accept loop, session state machine/pump, guardrails, two-phase drain) is now internal to `Database.Sql` behind the sealed `SqlDatabaseServer` (the abstract base merged away — an internal base with one same-assembly deriver is ceremony); `DatabaseServerOptions` renamed `SqlDatabaseServerOptions` (per-model consistency; `AddSqlServer(engine, Action<SqlDatabaseServerOptions>)`). **Root contracts untouched** — `IDatabaseServer`/`IDatabaseServerContext`/`IDatabaseServerSession` remain the only area-wide requirement; future model servers implement them directly against `Connections` + `Protocol` (via the root). **Extraction trigger recorded** (area DESIGN decision log + Sql DESIGN): on the SECOND model server, extract the then-proven common core — predicted session table + guardrails + drain, NOT the execute pump — into a shared library, with evidence; model #2's implementer extracts instead of copy-pasting. The 17 server tests moved into the Sql suite (display names `[Database.Sql]`; the external-derivability fixture `TestDatabaseServer` dropped — internalization removed its purpose; constructor validation now exercises `SqlDatabaseServer.Create`). Wiring: 3 slnx − Server (+ Sql docs entries), CI matrix 20→19, `App.Database` manifest − `Database.Server` (both Release packs green). |
| 2026-07-13 | #907, #908, #909, #910 | (fable5 hardening PR — MVCC batch) | **The MVCC integration — all four §3.8 steps implemented in the SQL engine** (four commits in dependency order on the same PR). **#907 binding:** every statement (explicit + auto-commit) runs under an `ITransactionContext` from a per-DATABASE manager; ONE sequence namespace (manager allocates from the storage counter via the new `TransactionManager.Create` allocator hook + `IStorage.ReserveTransactionSequence`/`BeginTransaction(long)` adopt overload); header-persisted sequence floor (sequences never recycle across checkpoint+reopen); classification-safe checkpoints (truncating record carries in-flight logical sequences; `TransactionRecovery.Analyze` reads them back; strategies defer the open-time checkpoint so recovery analysis precedes truncation); `ReadCommitted` = per-statement snapshot re-capture, `Snapshot` = fixed at begin, `Serializable` REJECTED (never weaker than requested); kernel aborts wrapped in new root `DatabaseTransactionAbortedException`. **#908 stamps + visibility:** 16-byte writer/deleter stamp header ahead of the tuple payload (record-space format v2, catalog-persisted marker, idempotent in-place v1 upgrade at open); version chains live IN the record space (update = same-length in-place tombstone + insert; delete = tombstone) — WAL-covered, restart-correct by construction; scans filter through the statement snapshot (dirty-read window CLOSED); crash suite proves aborted writers never resurrect. **#909 row-grain conflicts:** two-phase write statements — row/table locks through `ILockManager` first (async waits, cancellation-honoring, requester-closes-cycle deadlocks), then apply under a per-database gate in PER-STATEMENT storage brackets (the §3.8 migration option; non-durable inner commits — the transaction's commit record owns durability via journal ordering); latest-state re-validation under the lock (first-updater-wins; no write-skew); disjoint-row writers on one page BOTH COMMIT; rollback became logical via the new `SqlRecordSpaceVersionStore` (`IVersionStore` over the record space — the ledger drives physical undo + open-time bulk scrub); deadlock victims = new root `DatabaseTransactionDeadlockException` (retryable; `ExecutionFailure` on the wire; session stays usable); DDL interlocks via X/IX table locks. **#910 purge activation:** `SqlVersionPurgeWorker` live — aborted-undo retries + physical reclamation below the SAFE bound (min snapshot floor of open transactions, anchored above the recovered namespace — stricter than `OldestActive` alone, pinned-snapshot proof); index-maintenance remains the only sanctioned stub. Suites at tip: Sql 62, Transactions 30, Storage 64, Sql.Storage 14, Sql.Catalog 6, Indexing 15, Hosting 11, Server 17, Client 11, Sql.Client 12, Embedded 4, Execution 10, Application E2E 17 (TCP + real-process gateway). |
| 2026-07-13 | #862 scoping (+#907–#910 filed) | (fable5 hardening PR) | **Transaction/MVCC integration design + next-iteration root seams** (docs + one contract + backlog; NOT the MVCC implementation). The area DESIGN gains §3.8 — the isolation split-brain stated plainly (`Database.Transactions` ships a full MVCC manager NO engine uses; the SQL engine serializes at page grain with no snapshot filtering for readers; R1's "isolation via MVCC snapshots" not yet true end-to-end; the version-purge worker is a stub for exactly this reason) — and the four-step integration architecture: session↔manager binding via `IStorageTransactionSource` pairing (storage transactions stay the physical WAL bracket, MVCC above), row writer/deleter stamps in the record space (B+Tree leaf-entry precedent), snapshot-visible scans, row-grain write conflicts via hashed-key locks (B+Tree uniqueness precedent) with deadlock surfacing, purge activation (`PurgeWriterAsync` + `OldestActive` bound). Matching next-iteration sections in the Transactions/Sql/Storage DESIGN docs. **Contract landed now (judicious):** the isolation-level seam — `IDatabaseSession.BeginTransactionAsync(IsolationLevel, …)` overload + `IDatabaseTransaction.IsolationLevel` (root consumes the Transactions child's enum, legal post-inversion; engines may run stronger, never weaker; SQL carries the level today, +4 isolation tests). No speculative MVCC contracts in the root. §3.9 scopes the other root seams as design notes: health (#168 — reads the redesigned observational surfaces, Web.Health shape), backup/restore (#161 — an engine-level per-model data-machine operation, not a host service), authorization (#177 — vocabulary grows in the `Database.Security` child root, per-model enforcement in model packages). **Filed #907/#908/#909/#910** under #862 (manual recipe — 5-segment parent). |
| 2026-07-13 | root redesign (owner-ratified) | (fable5 hardening PR) | **Engines are data machines, servers are per-model — the approved root redesign.** (1) Engine lifecycle members REMOVED from the root contract (reverses #903's `StartAsync`/`StopAsync`; the short-lived `IDatabaseEngineLifecycle` deleted before merge): engines are operational from creation, disposal = quiesce workers → durable flush → close; `EngineState` shrank to observational `Running`/`Faulted`/`Disposed` (worker-fault reporting kept a minimal `State`). The #902 claim handshake DELETED — `IDatabaseEngineWorker` is observational (name/kind/cadence), pumps live on the guided base for engine-internal use, engines spawn their loops on their own dedicated threads at creation (Lane-H guardrail satisfied engine-side). (2) **`Database.Server` resurrected** as shared server infrastructure above the root (records the partial reversal of the 2026-07-12 fold — per-model servers cannot derive from a base in Hosting, COHRES001): guided abstract base `DatabaseServer` (session state machine/pump, guardrails, two-phase drain) + `DatabaseServerOptions` (no Engines list — the derived server supplies its single engine); `Database.Sql` ships `SqlDatabaseServer : DatabaseServer` + the `AddSqlServer` verb; `Database.Hosting` is composition-only (refs = root + Hosting; Connections dropped; worker slot services, engine host service, and worker mapping options deleted). (3) Context goes plural, converged with Web: `IDatabaseApplicationContext` = `Servers` + `Engines`; `IDatabaseApplication` = `Context` + start/stop; builder's deferred factory retyped to `Func<IDatabaseApplicationContext, IDatabaseServer>`; multiple servers allowed. Wiring: new project in 3 slnx + CI matrix (20 projects) + `App.Database` manifest (both Release packs green). All 20 suites green (529: Server 17, Hosting 11, Sql 31 reworked to creation/dispose + data-machine semantics; TCP + real-process gateway E2Es intact). |
| 2026-07-13 | design hardening (owner-directed) | (fable5 hardening PR) | **Child-root inversion — the root rolls up its base components.** `Assimalign.Cohesion.Database` now references all nine child roots (`Types`/`Language`/`Storage`/`Transactions`/`Execution`/`Indexing`/`Protocol`/`Security`/`Governance`); no child root references the root, so each stays independently consumable (the point of breaking the vast base surface out). Moves: `ProtocolVersion` home to `Protocol` (plain static `Current`; the root/extension-member split existed only because Protocol→root made root→Protocol impossible); `TransactionId`/`TransactionState` down to `Transactions`. Error model: `ProtocolException`/`TransactionAbortedException` re-rooted on `Exception` (child roots own independent exception roots; `DatabaseException` = the root and everything above it; wrap-at-model-boundary policy recorded) — catch-site audit found NO live wire-behavior change. Hardening win: `Database.Hosting`'s COHRES002 exemption REMOVED (Protocol/Security arrive transitively via the root). `Governance` graduated into the `App.Database` manifest (it is in the root's closure); both Release packs green. `Indexing` joined as the NINTH child root in a follow-up commit on the same PR (owner direction): its only root coupling was `DatabaseException` ancestry, re-rooted onto a new `IndexException` (4 throw sites + 2 test assertions + crefs); Indexing/Sql.Catalog/Sql/Hosting suites re-verified green. Rulebook: `resource-areas.md` child-root paragraph states the root→child arrow rule; `Build.Rules.targets` comment kept consistent. All 19 area suites green (528). |
| 2026-07-13 | builder pattern (owner-directed) | (fable5 hardening PR) | **`IDatabaseApplicationBuilder` fleshed out to the Web-area pattern** (the owner's stub on the root; pattern recorded in `resource-areas.md` as the cross-area expectation). Root seam: `AddEngine`, `AddServer` (instance + deferred factory receiving the FINAL engine list at `Build`), `Engines`, `Build()`; `IDatabaseApplication` aligned to multi-engine (`Engines` + start/stop). Hosting implements: `DatabaseApplicationBuilder` over `DatabaseApplicationOptions` (hosting-only surface on `builder.Options`), `DatabaseApplication.CreateBuilder()` mirrors `WebApplication.CreateBuilder()`, `DatabaseApplication : IDatabaseApplication`. `Database.Sql` ships the first model verb `AddSqlDatabase(...)` (`extension(IDatabaseApplicationBuilder)`, returns the registered engine, zero hosting reference — models register without knowing the hosting layer). `Database.Application` composes through the builder (proof-of-pattern; TCP + gateway E2E ride it). +6 tests (Hosting 28, Sql 36); all 19 suites green (534). |
| 2026-07-13 | #906 (new, pin) | (fable5 hardening PR) | **`<Area>.Application` / `<Area>.ApplicationModel` conventions documented** (owner direction). `resource-areas.md`: the standing exemption conventions are now THREE per area (`.Testing`, `.Application`, `.ApplicationModel`, each with its reason), plus a new section on the `<Area>.Application` convention — its TARGET design is the **manifest-generation project** (SDK consumers load it; build tasks code-gen the application manifest; the SDK-specific ApplicationModel surfaces it to the gateway), NOT an executable; the same convention in every area. The current `Database.Application` executable is the documented interim state, **pinned on the ApplicationModel program via #906** (filed, not closed). Mirrored in the area README/DESIGN and `Database.Application/docs/DESIGN.md` ("Target design (pinned)"); `Build.Rules.targets` comment header kept consistent. Docs-only + the pin issue. |
| 2026-07-12 | #902 | (fable5 run-sql PR) | Stage 4, engine self-sufficiency (#862) — **engine lifecycle + engine-owned background workers.** (1) `IDatabaseEngine.StartAsync/StopAsync` on the root contract (idempotent start, stop = quiesce + durable flush + close, restartable; all five engines implement); `Database.Hosting` drives engine lifecycle via an internal per-engine host service registered FIRST (engines start before the worker slots + endpoint, stop last). (2) The worker seam: `IDatabaseEngineWorker` (+ guided base) — kind/cadence, blocking pump `Run` for dedicated threads, bounded `RunIteration` for timer loops, atomic **claim** handshake; `IDatabaseEngine.Workers` exposes the inventory from creation. Ownership: hosts claim at composition time (before start); engine start self-schedules every unclaimed worker on its own threads; stop quiesces + releases; a worker can never run twice. SQL engine ships the five-row inventory: signal-driven WAL group-commit flusher, paced page write-back, checkpointer (BOTH file sets per db, busy = skip/retry), version-purge + index-maintenance as documented stubs (no MVCC store / no compaction yet — sanctioned). Storage grew the worker seams: `StorageCommitDurability.Grouped` (group-commit gate: one worker fsync covers all pending commits, bounded-window **self-help** = durability never hostage to the worker; synchronous stays default), `IStorage.FlushPendingCommits`/`WriteBackDirtyPages` (write-ahead gate honored), and the checkpoint interlock hardened to an active-transaction count held across the whole checkpoint (begun-untouched txns now block truncation — caught by test). Hosting slots stopped parking: `WriteAheadFlushService`/`PageWriterService` drive claimed workers on dedicated threads, new `CheckpointService`/timer services tick pooled rows, `DatabaseApplicationOptions.Workers` = per-kind enable + execution model (cadence stays on ENGINE options — anti-drift). Tests: Storage 61, Sql 34 (incl. hostless embedded-parity R10 proof w/ restart recovery), Hosting 24, + Transactions/Indexing/Sql.Storage/Client/Sql.Client re-verified. |
| 2026-07-12 | #904 (new, discovered) | (fable5 run-sql PR) | Stage 4 — **the standalone host executable + the definitive SQL-over-the-host E2E (closes the #853 deferral).** New `resources/Database/Assimalign.Cohesion.Database.Application` (`OutputType=Exe`, `IsPackable=false`) = exactly the artifact `DatabaseResource` declares; thin `Program` over an internal testable bootstrap: `DatabaseHostConfiguration.FromEnvironment()` → file-backed `SqlDatabaseEngine` at the data path, durability convention mapped (`full`/`synchronous`→per-commit fsync; `grouped`/`relaxed`→group-commit; unknown = loud exit 1), TCP on ALL interfaces at the injected port (OS-assigned when unset), `DatabaseApplication` + a `DefaultDatabaseProvisioner` (`app`, create-if-missing before the endpoint — the wire protocol has no CREATE DATABASE verb); graceful SIGINT/SIGTERM drain via `PosixSignalRegistration`. **Sanctioned COHRES001 exemption** (`CohesionHostingIsolationExemptions=Database.Hosting` — the composition-root executable is the analog of a user application; rationale in-csproj). Deliberately NOT in the `App.Database` manifest (frameworks deliver libraries; documented in-manifest). Wired: 3 slnx + CI matrix (19 projects) + area README/DESIGN. **NativeAOT evidence:** `dotnet publish -r win-arm64 /p:PublishAot=true` green (VS dev shell + vswhere + subst short path); the 3.4 MB native exe runs and serves its TCP port. Tests 17: bootstrap mapping/rejection matrix + startable-app + the definitive E2E (real TCP loopback: DDL, parameterized INSERT/SELECT, explicit txn commit/rollback via the composed engine session — wire transaction frames remain a documented protocol deferral — restart recovery, grouped-durability round-trip, checkpoints under load w/ journal-truncation proof) + the previously-deferred **real-process `LocalGateway` E2E**: `AddDatabase(...)` → LocalGateway resolves the apphost from the test output, launches the actual process (readiness marker), SQL round-trips over TCP, forceful-kill relaunch on the same mount recovers the data (WAL). |
| 2026-07-12 | review revision (#166 fold · rename · #902 plan) | (fable5 service-surface PR, revision) | Three-commit revision batch on the open PR. **(1)** `IJournal`→`IStorageJournal` + guided base `Journal`→`StorageJournal` (`Database.Storage`; `StreamJournal`/`JournalRecord`/`JournalException` keep their names; public `TransactionRecovery.Analyze`/`TransactionLog.CreateJournalBound` signatures updated). **(2)** `Database.Server` folded into `Database.Hosting` and deleted (owner decision — the server runtime is a hosting concern; supersedes the `CreateHostService` cross-assembly seam): sanctioned COHRES002 `CohesionHostingIsolationExemptions` for `Database.Protocol`+`Database.Security`; `DatabaseApplicationOptions.Server` composes the endpoint directly; manifest/CI/slnx/docs rippled (18 delivered projects). **(3)** `Database.Hosting` DESIGN gains the #902 execution-model mapping (engine-owned WAL flusher + page writer on `DedicatedThreadService`; checkpointer/version-purge/B+Tree maintenance as `BackgroundService`; endpoint hosting-owned; idle sweep + deadlock detection deliberately not host slots) and #902's body carries the same inventory as acceptance criteria. **(4, follow-up in the same batch)** server seam promoted to the area root (owner decision): `IDatabaseServer`/`IDatabaseServerSession` + the `ProtocolVersion` value type moved to `Assimalign.Cohesion.Database` (the Web area's shape — COHRES001 makes `Database.Hosting` unreferenceable by area libraries, so the seam must live where quotas/health/#167/#168 and a future `Database.Testing` can see it); `ProtocolVersion.Current` stays with `Database.Protocol` as a C# 14 static extension member, so no call site changed. |
| 2026-07-12 | App.Database framework + CI (plan task) | (fable5 service-surface PR) | Stage 4 delivery wiring. **Framework manifest:** the `App.Database` ItemGroup in `frameworks/Assimalign.Cohesion.App.props` grew from umbrella+root+private-Web to the full **shipping set** — kernel (`Database`/`.Types`/`.Language`/`.Storage`/`.Transactions`/`.Indexing`/`.Execution`), service surface (`.Protocol`/`.Security`/`.Client`/`.Embedded`/`.Hosting` — the server runtime ships inside `.Hosting` after the fold), the SQL family (`.Sql`/`.Sql.Language`/`.Sql.Catalog`/`.Sql.Storage`/`.Sql.Client`), and the `.ApplicationModel` manifest — plus the outside-area `Assimalign.Cohesion.ApplicationModel` on the **Ref pack** (the AddDatabase surface exposes its types → CS0012 otherwise; base App doesn't carry it yet, remove when it graduates there). `Web` stays the private assembly. Excluded + documented in the manifest: the source-less Documents/Graph/KeyValuePair/Blob/Cache model scaffolds, and post-MVP contract shells (`Governance`/`Replication`/`Sql.Replication`/`Sql.Security`/`Memory`). **Validated by green packs** of BOTH `App.Database.Runtime` and `App.Database.Ref` (the collection target hard-fails on any listed-but-missing assembly). **CI:** `.github/workflows/resource-database.yml` matrix expanded from 1 to all 18 delivered Database projects across ubuntu/windows/macos, so the COHRES001/002 hosting-isolation guard runs per-project in CI (esp. `Database.Hosting`). `Install-Local.ps1` already carried `App.Database` + `Sdk.Database` (no change). `FrameworkList.xml`/`RuntimeList.xml` untouched (build artifacts). |
| 2026-07-12 | #853 | (fable5 service-surface PR) | Stage 4, Lane H — **the ApplicationModel orchestration manifest + AddDatabase integration**. `Database.ApplicationModel` build-out (manifest-only; references the declarative ApplicationModel plane, **zero runtime references**). `DatabaseResource` now injects the **conventional host env vars** — `COHESION_DATABASE_DATA_PATH`/`_ENDPOINT_PORT`/`_DURABILITY` (public constants on `DatabaseResource`) — the same names `Database.Hosting`'s `DatabaseHostConfiguration` binds (the two planes agree by convention without sharing an assembly): data path = mount path; **port set only when explicitly declared** (a platform-allocated port is observed post-launch and injected by the gateway); durability when configured; caller env vars win. New `DatabaseResourceOptions.Durability`. **Integration test** drives the manifest through the generic `ApplicationGateway` realization algorithm (which `LocalGateway` inherits) over a recording controller + minimal state manager: `AddDatabase(...)` + a dependent that `DependsOn` it realizes the database FIRST, gates the dependent on `Running`, publishes the database's **observed endpoint** (concrete allocated port) for the dependent, tears down in **reverse order**, and marks dependents **`Blocked`** when the database fails (startup aborts). 8 tests (3 original + 2 env-var + 3 orchestration). **Deferred:** a `LocalGateway` E2E that launches the real host *process* needs the packaged `Assimalign.Cohesion.Database.Application` executable on disk (host-packaging work, outside a manifest unit test) — the generic-algorithm coverage proves every gateway-observable manifest behavior. DESIGN updated with the env-var bridge + realization behavior; docs/tests already slnx-wired. |
| 2026-07-12 | #166 | (fable5 service-surface PR) | Stage 4, Lane H — **the database host composition**. `Database.Hosting` build-out on the `libraries/Hosting` execution menu: `DatabaseApplication : Host<DatabaseApplicationContext>` composes `WriteAheadFlushService` + `PageWriterService` (`DedicatedThreadService`) then the composition root's endpoint (and other) `IHostService`s, so endpoints start last and **drain first** on stop. **COHRES002 discovery + final resolution (revised 2026-07-12, same PR):** the hosting module may reference no same-area library but the root, so it could not name `IDatabaseServer` — the first delivery homed the endpoint adapter with the server (`DatabaseServer.CreateHostService`). **Superseded by the fold (owner decision):** `Database.Server` merged INTO `Database.Hosting` (the server runtime is a hosting concern; mirrors Web.Server→Web.Hosting), with the sanctioned per-project `CohesionHostingIsolationExemptions` for `Database.Protocol`+`Database.Security` (the server's own machinery; Execution/Types stay transitive via the root). `DatabaseApplicationOptions.Server` now carries the `IDatabaseServer`; the app wraps it in the internal `DatabaseServerHostService : BackgroundService`, registered last (starts last, drains first); `CreateHostService` internalized, `DatabaseServer.Create` stays the public manual-composition path. **Engine-worker descope (R10):** `WriteAheadFlushService`/`PageWriterService` are documented no-op placeholders that park until shutdown — the SQL engine flushes synchronously at commit and owns its durability (embedded consumers must get it without a host), so there is no host-driven flush/page-writer work yet; the engine-owned background-worker seam + an `IDatabaseEngine` start/stop lifecycle seam are **filed as #902** under the engine self-sufficiency feature #862 (advances #862, not closed here). Env-var conventions bound by Hosting: new `DatabaseHostConfiguration.FromEnvironment()` reads `COHESION_DATABASE_DATA_PATH`/`_ENDPOINT_PORT`/`_DURABILITY` (the #853 manifest sets the same names). 6 host tests (served round-trip through the composed endpoint over `Connections.InMemory`, default+toggled composition, drain-to-Stopped, env-var bind + malformed-port reject); after the fold the 16 server tests join Hosting/tests (22 total). Hosting DESIGN/OVERVIEW absorb the server design record; wired into 3 slnx files. |
| 2026-07-12 | #180 | (fable5 service-surface PR) | Stage 4, Lane MS — **the typed SQL client**. New `Database.Sql.Client` from a bare csproj: a thin relational surface over the shared `Database.Client` core (references `Database`/`Database.Client`/`Database.Types`/`Connections` — deliberately **not** `Sql.Language`: the client never parses SQL, the server owns that via the root text-execute seam). `SqlClient.Create(SqlClientOptions{Settings, ConnectionFactory, Observer})` → `ISqlClient.ConnectAsync` (rents a pooled authenticated session off the core); `ISqlConnection` with `QueryAsync`/`ExecuteAsync`/`ExecuteScalarAsync<T>` in both `SqlCommand` and string+params forms; `SqlCommand`+`SqlParameterCollection` (binds by **bare name** — the `@`/`$` sigil is normalized away on add); `SqlResultSet : IReadOnlyList<SqlRow>` projecting the core's boxed `DatabaseClientResult` into typed `SqlColumn`/`SqlRow` with ordinal+name access and **widening numeric getters** via `Convert.ChangeType` (IConvertible fast path, AOT-safe — Int32 read as long "just works", genuine mismatch = `InvalidCast`). Error surface: `SqlClientException : DatabaseException` mapping every `ProtocolErrorCode` onto a `SqlClientErrorKind` taxonomy (parse/execution/connection/auth/unavailable/…) + a `ConnectionUsable` flag encoding the core's statement-level-failure-keeps-session rule; wire `Code` preserved. Telemetry: `ISqlClientObserver` (primitives, allocation-free, defensively invoked so a throwing observer never faults a command; attached via options — no DI). 12 E2E tests over the real server + a live SQL engine on `Connections.InMemory`: typed columns/rows, numeric widening, command+params round-trip, affected count, `COUNT(*)` scalar, IsNull, unknown-column reject, parse-vs-execution kind mapping + connection survival, observer executing/executed/failed, pool session reuse, options validation. Docs OVERVIEW/DESIGN; slnx docs+tests wiring (3 files). |
| 2026-07-12 | #852 (complete) | (fable5 batch PR) | Stage 1/4, Lane P — **completes #852**: the server runtime + shared client core on the protocol slice. `Database.Server` runtime: `DatabaseServer.Create(options)` (options gain Engines + a **bound `IConnectionListener` instance** — host owns/disposes the listener, server only accepts; `Authenticator` seam defaulting to allow-all) driving the full session state machine (Connected→Startup→Authenticating→Ready⇄Executing→Terminated) with every DoS guardrail live: unknown-major → `UnsupportedVersion`+close, `MaxSessions` counts handshaking sessions (over-limit = `Unavailable` at accept, never a session), `AuthenticationTimeout` drops silently, `IdleTimeout` evicts w/ `Unavailable`, two-phase StopAsync (soft-stop cancels idle reads at frame boundaries, in-flight executes get the drain budget, then hard abort). **Root-contract text-execute seam**: `IDatabaseSession.ExecuteAsync(string, params)` added so the server never parses model languages (SQL session → `SqlQueryRequest.FromSql`); NEW root `DatabaseParseException` gives the server the ParseFailure-vs-ExecutionFailure wire mapping without message sniffing (statement errors keep the session Ready; protocol violations = Error+close). MVP authenticate exchange = empty-payload trust challenge + opaque evidence bytes → NEW `Database.Security` `IDatabaseAuthenticator` (+`DatabaseAuthenticator.AllowAll`). NEW `Database.Types.DatabaseValueCodec` (runtime-type↔component bridge; one codec for params AND rows on both wire ends — anti-drift). `Database.Client` from bare csproj: `DatabaseClient.Create` → `IDatabaseClient.RentAsync` pooling (slot semaphore + idle stack; **reuses authenticated sessions across rents** — proven by same-instance + single-server-session test; only ParseFailure/ExecutionFailure leave a connection poolable), `IDatabaseConnection.OpenAsync/ExecuteAsync` w/ materialized `DatabaseClientResult` (documented MVP: drain-before-reuse), `DatabaseConnectionSettings.Parse` key=value; string (Database/Principal/Endpoint=host[:port]/MaxPoolSize; driver = typed `IConnectionFactory`, never in the string — AOT static composition; typed `EndPoint` override for non-network transports), `DatabaseClientException` carrying the wire `ProtocolErrorCode`. E2E acceptance over `Connections.InMemory` against a live `SqlDatabaseEngine`: 16 server tests (handshake, version/database/auth rejection, SELECT header+rows+complete streaming, params by bare name, ParseFailure/ExecutionFailure keep-alive, MaxSessions, auth+idle timeouts, ping/pong, terminate, drain) + 11 client tests (typed rows, params, affected counts, wire-code mapping + connection survival, pool reuse, pool-exhaustion wait, capacity `Unavailable`) + 3 Types codec + 1 Sql text-seam. Docs: NEW root `Database` + `Database.Security` + `Database.Client` OVERVIEW/DESIGN; Server/Protocol/Types/Sql DESIGN updated; slnx docs wiring. Re-verified: Protocol 8, Execution 10, Sql 18, Sql.Language 156, Types 19. Gotcha: `resources/Directory.Build.targets` compile-excludes `Shared/**` in test projects — helpers live in `TestObjects/`. |
| 2026-07-11 | #852 (partial) | (fable5 batch PR) | Stage 1/4, Lane P — the protocol slice only (**does not complete the item**; the server runtime and Database.Client remain). Stream-based `IProtocolFrameReader`/`Writer` implementations via `ProtocolFraming.CreateReader/CreateWriter` (payload bound enforced from the header before ANY allocation; clean null on end-of-stream between frames; loud `ProtocolException` mid-frame), and the first payload schemas as wire contract: `ProtocolStartupMessage` (version+database+principal), `ProtocolErrorMessage` (stable code+text), `ProtocolExecuteMessage` (statement + named params as shared tuple-codec bytes — one parameter encoding for all five models), `ProtocolResultHeaderMessage` (column names + shared type-identity bytes), `ProtocolResultCompleteMessage` (affected count); `ResultRow` payloads = raw tuple-codec bytes by design (no wrapper). 8 tests: ordered round-trips over a stream, truncation + oversized-declaration rejection, message codec round-trips, malformed-payload failures. NEXT session: `Database.Server` session state machine over Connections.InMemory + `Database.Client` core w/ pooling per the Server DESIGN.md. |
| 2026-07-11 | #178, #179 | (fable5 batch PR) | Stage 3, Lane MS — **the SQL engine now executes real SQL end-to-end**. Plan/execute split: `SqlPlanner` binds AST↔catalog into a thin `SqlPlan` IR, rejecting out-of-surface features at plan time w/ precise messages (JOIN/GROUP BY/subqueries/aggregates-beyond-lone-COUNT(*)/INSERT..SELECT); `SqlPlanExecutor` runs scans filtered by object-id-prefixed rows (shared tuple-codec records, `SqlRowCodec`; ADD COLUMN O(1) via null-tail decode, **DROP COLUMN rewrites rows** — positional-layout bug caught by test), SELECT w/ WHERE + projection + stable ORDER BY (nulls-first) + LIMIT/OFFSET + DISTINCT + lone COUNT(*), INSERT w/ defaults+nullability, UPDATE/DELETE w/ accurate counts + relocate-on-growth, DDL→catalog; `SqlExpressionEvaluator`: SQL null propagation, 3-valued AND/OR, numeric promotion, ordinal strings, hand-rolled LIKE, CASE/BETWEEN/IN/IS NULL, @params bound by bare name (sigil kept in AST — fixed), COALESCE/UPPER/LOWER/LENGTH/ABS. Engine: 2 file sets per db (data + `.catalog`) via the existing strategy seam; parameter-bridge executor + old iterator result types DELETED; typed `SqlMaterializedResultSet`/`Row`; `SqlQueryRequest.FromSql`. DDL-parser fix: multi-arg parameterized types (`DECIMAL(18, 4)`) consumed to the closing paren. Tests REWRITTEN to real SQL: 17 engine (lifecycle, txn rollback incl. session-dispose, DDL flow, defaults, deterministic SELECT, params, DISTINCT/NULL semantics, typed round-trips incl. GUID/decimal, unsupported-feature messages, in-memory strategy) + 2 durability (rollback, restart recovery) — Sql.Language 156 re-verified. |
| 2026-07-11 | #175 | (fable5 batch PR) | Stage 2, Lane MS. `Sql.Catalog` built from its empty placeholder: metadata model (`SqlCatalogTable` w/ stable catalog-assigned `ulong` ObjectIds, `SqlCatalogColumn` typed by `DatabaseTypeInfo` + nullability + default literals, PK constraint), `ISqlCatalog` with **self-committing transactional DDL** (create/drop table, add/drop column — each its own WAL-durable storage txn; DDL-in-DML deliberately unsupported, documented), persistence on a **dedicated catalog storage file set** (`<db>.catalog` convention — isolation from user-row scans while reusing page/WAL durability), records encoded with the #854 self-describing tuple codec (one record per table; in-place rewrite w/ relocate-on-growth; counter record monotonic across reopen incl. torn-update guard), index-registration persistence (`Save/GetIndexRegistrations` bridging `IIndexRegistry` — physical/schema split documented both sides), `SqlCatalogException` root. Language-free by design (AST→catalog translation is the planner's job). 6 tests: identity assignment, case-insensitive lookup, duplicate/PK validation, full-attribute reopen persistence (via WAL replay — no page flush), alter add/drop w/ guards, registration round-trip, 50-table growth/relocation. slnx wiring (3 files) + docs. |
| 2026-07-11 | #169, #170, #171, #172, #173, #174 | (fable5 batch PR) | Stage 2, Lane LS. SQL language stabilized as a declared dialect: **namespace-rule fix** (all types now `Assimalign.Cohesion.Database.Sql.Language` matching the assembly — 58-file sweep incl. Database.Sql consumers, done before external consumers exist); `docs/DIALECT.md` = the authoritative supported/recognized/rejected statement matrix + literals + type-name table + builtin list + diagnostics contract (#172); `SqlTypeNames` single SQL-name→`DatabaseType` translation table incl. DECIMAL(p[,s]) argument-is-precision rule (#173); AST stabilization (#170): raw statement text stamped on the root after parse, string literal nodes now carry the VALUE (quotes stripped, `''` unescaped) not the lexeme, offsets-are-positions/line-mapping-is-presentation documented; conformance corpora (#171/#174): 18-statement supported corpus (parse clean + text stamped), 24-input malformed/hostile corpus (total parsing, never throws), literal-form typing tests, 23-name type-resolution matrix + constraint-mapping tests, 8 builtin-recognition tests; assertion-free lexer smoke test replaced with a real classification test. 156 tests green; Sql engine 27 re-verified. |
| 2026-07-11 | #851 | (fable5 batch PR) | Stage 2, Lane T. B+Tree over shared pages: one node/page sorted-directory layout on `PageType.Index` (`MaxKeyLength` 1 KiB), recursive leaf/internal splits w/ root growth, sibling chain for range scans; MVCC leaf entries (writer+deleter stamps; tombstone deletes; aborted stamps revert physically via page images); ALL page mutations via NEW public `IStorage.OpenPageForWrite`/`AllocatePageForWrite` (before-image coverage ⇒ crash-mid-split reverts, committed splits replay — both crash-tested); **uniqueness = latest-state check under an exclusive hashed-key lock (inserts AND deletes)** — snapshot-visibility checks allow write skew, caught by test; tree-level RW latch, cursors materialize under read latch; `IStorageTransactionSource` pairs contexts w/ storage txns; directory persistence deliberately catalog-owned (`IIndexRegistry`/`BTreeIndexRegistration` export + `ExistingIndexes` re-attach); `IndexKey.From(DatabaseKeyWriter)` bridges #854 composite keys. 15 tests: point/range/reverse+inclusivity, 2000-entry split ordering, 1000-random-key property scan, composite significance property, MVCC invisibility/snapshot pinning, tombstone snapshots, unique reject/re-insert, concurrent unique writers serialize via lock manager, crash-mid-split revert, committed-splits-survive-restart (no page flush), manager lifecycle. |
| 2026-07-11 | #163, #164, #165 | (fable5 batch PR) | Stage 1, Lane X. Execution substrate: `QueryExecutionContext` (request + scope + RequestAborted + thread-safe diagnostics + item bag), middleware-shaped `IQueryPipeline`/`IQueryPipelineStage`/`QueryPipelineBuilder` (stages wrap/short-circuit; operator trees stay in model planners per lane guardrail), `IQueryTransactionScope` boundary seam (Execution can't reference Transactions — root→Execution + Transactions→root would cycle; engines adapt), boundary rules enforced at the pipeline edge: implicit+Success→commit, implicit+failed-result→rollback-but-return, exception/cancellation→rollback-then-propagate (rollback failure faults scope, never masks root cause; rollback uses CT.None so aborted requests still release), explicit scopes untouched. Concrete `QueryStatementResult`; `QueryExecutionException` local root; tests csproj was an empty shell — now real. 10 tests: ordering, short-circuit, commit/rollback boundaries, error propagation, rollback-failure masking, explicit-scope hands-off, pre/mid-execution cancellation, diagnostics accumulation. |
| 2026-07-11 | #850 | (fable5 batch PR) | Stage 1, Lane T — the ACID substrate. Implementations for all five contracts: `TransactionManager.Create` (sequence assignment, live active table, per-access ReadCommitted snapshot refresh vs fixed Snapshot; commit awaits durability while still in the active table then releases locks as a set; durability failure ⇒ purge+`TransactionAbortedException`+Faulted; `OldestActive` prune bound), `LockManager.Create` (S/U/X/IS/IX matrix, same-owner upgrades, FIFO wake, wait-for-graph deadlock detection, requester-closes-cycle victim policy, cancellable waits), `VersionStore.CreateInMemory` (newest-first chain resolution, prune-below-oldest-active, `PurgeWriterAsync` — NEW contract member: snapshot has no commit-log awareness so the store must unlink aborted writers), `TransactionLog.CreateInMemory`/`CreateJournalBound(IStorageJournal)` (commit = append + `EnsureDurable`; group commit via shared fsync), `TransactionRecovery.Analyze` (committed iff durable commit record). 22 new tests (27 total): snapshot-vs-readcommitted visibility, own-writes, rollback purge, lock release on commit, durability-failure abort, compat matrix/upgrade/FIFO/deadlock-victim/cancellation, prune/purge, journal-bound crash-mid-commit atomicity. |
| 2026-07-11 | #854 | (fable5 batch PR) | Stage 1, Lane T. Database.Types fleshed out: explicit `Collation` identity (binary = code-point order matching UTF-8 bytes — not CompareOrdinal; invariant = culture-invariant linguistic; ids persisted in keys, sealed set = persistence contract), order-preserving self-describing key codec `DatabaseKeyWriter`/`DatabaseKeyReader` (tag byte per component: nulls first, cross-type by tag; sign-flipped BE integers; IEEE-754 total-order fold w/ positive-NaN canonicalization — .NET double.NaN is negative-signed, caught by test; decimal = normalized scientific digits w/ complemented negatives; linguistic strings = sortkey + round-trip original; zero-escaped var-length payloads; DateTime kind preserved non-ordering; Guid RFC-4122 BE), `DatabaseTypeException` root. Empty placeholder classes (block-scoped ns) deleted; csproj cleaned; NEW tests project wired into all 3 slnx files; docs OVERVIEW/DESIGN. 16 ordering/round-trip tests incl. full-range integers, ±∞/−0.0/NaN, decimal scale mix, embedded-zero binaries, composite significance, prefix pairs. |
| 2026-07-11 | #160, #162 | (fable5 batch PR) | Stage 1, Lane K — the durability keystone. WAL v2: typed binary records (begin/commit/rollback/checkpoint/before-image/after-image/logical-op; long txn sequence, string-heavy logical journal removed), `IStorageJournal`+`StreamJournal` with CRC frames + torn-tail tolerance. Storage transactions (`IStorageTransaction`): before-image at first touch, buffer-pool write-ahead gate (steal-safe), commit = after-images + commit record + fsync (no-force), rollback restores pages in memory, page-level single-writer locks. Recovery = last-record-wins over committed after-images ∪ uncommitted before-images, exact-LSN idempotency, runs on open, ends in checkpoint; `Checkpoint()` = durable flush + journal truncation with continued LSNs; clean shutdown checkpoints. SQL engine rewired onto storage transactions (parameter-bridge executor keeps behavior; **rollback now actually undoes in-memory changes** — prior defect). 10-test crash suite (`CrashSimulationStream` harness: flush-gated journal, write-through data): redo-without-page-flush, crash-before-commit with stolen writes, stolen-update undo, repeated-replay idempotency, rollback+crash, clean-shutdown checkpoint, interleaved txns, page write-conflict, completed-txn guards, checkpoint-vs-active-txn; + 9 journal codec/ordering tests; + 2 SQL-level durability tests (rollback visibility, engine-restart recovery). 55 Storage / 27 Sql / 14 SqlStorage green. |
| 2026-07-11 | #158, #159 | (fable5 batch PR) | Stage 1, Lane K. Buffer pool build-out: true LRU eviction over unpinned entries (pins/hits touch to MRU; pinned pages skipped; all-pinned overflow fails loudly), buffer reuse via a recycle stack of pinned 8 KiB buffers (steady-state churn allocates nothing; fresh pages zeroed on reuse), failed/corrupt loads never poison the cache, page-manager allocation recycles freed pages and guards unallocated reads. 12-test suite: LRU order, pinned-eviction refusal, dirty write-back w/ checksum, stale-buffer leakage, corrupted-load cache hygiene, 8-worker pin/unpin churn, concurrent writer persistence, alloc/free/realloc lifecycle. |
| 2026-07-11 | #157 | (fable5 batch PR) | Stage 1, Lane K. Storage model aligned: page header redone (proper 4-byte CRC-32 + 8-byte page LSN; removed the checksum/PageId union hack), `PageType` fixed (`Free = 0` discriminator, `[Flags]` removed), `PageFlags` normalized, CRC verified on every buffer-pool load / stamped on every write-back (`StorageCorruptionException`), file header moved into page 0's body so page 0 is checksummed like every page (+`LastCheckpointLsn` field), free-space map rebuilt from page headers on open (freed pages recycle across reopen; O(1) `IsAllocated`), oversized-record guard, dead `IStorageIndexManager`/`IStorageCatalogManager` stubs removed (Indexing DESIGN.md updated), `JournalException` joined the `StorageException` root; Storage docs (OVERVIEW/DESIGN) created; 10 new alignment tests. |
| 2026-07-06 | #855 | (this PR) | Stage 0. Area scaffold: `resources/Database/DESIGN.md` (requirements + gap analysis + decision log) and README; new projects `Database.Transactions` (MVCC snapshot implemented+tested), `Database.Indexing` (IndexKey encoding implemented+tested), `Database.Protocol` (frame codec implemented+tested), `Database.Server`, `Database.ApplicationModel` (AddDatabase working), `Database.Sql.Replication`; model-root contracts + engine shells for Documents/Graph/KV/Blob; `Sdk.Database` migration targets + task skeletons; fixes: `StorageModel` restored (part of #157 — alignment remains open), `SqlDatabaseEngine.Model`, `Cataalog.csproj` typo, misplaced KVP.Security tests, IDE0011 breaks in Storage + Sql.Language. Filed #850–#854 (kernel features), #856 (tooling epic), #857–#859. Second pass (same PR): `EmbeddedDatabase` facade implemented + tested (platform data-layer requirement R10), enterprise requirements captured as R11, filed #861 (encryption at rest) and #862 (embedded consumption + engine self-sufficiency). |

---

## 6. Fast reference

- Architecture: `resources/Database/DESIGN.md` · Area catalog: `resources/Database/README.md`
- Epics: Core Engine **#31** · SQL **#4** · DocumentDB **#5** · GraphDB **#50** · KV/Blob/Cache **#57** · Tooling **#856**
- Fan-out prerequisites to land first: **#158, #160 → #850 → #851** (kernel spine) and **#852** (client spine)
- Gating decision: **#193** (graph query standard) — until decided, only #193 itself is workable in the Graph lanes
- Hosting model: `libraries/Hosting/.../docs/DESIGN.md` (execution menu) · Orchestration: `libraries/ApplicationModel/DESIGN.md`
- Platform data layer: other resources consume engines **embedded-first** via `Database.Embedded` (DESIGN.md §3.7); engines must stay self-sufficient (#862)
- Work-item mechanics: `.claude/skills/cohesion-work-items/` (`-EmitClosesBlock` for PR close-out)
