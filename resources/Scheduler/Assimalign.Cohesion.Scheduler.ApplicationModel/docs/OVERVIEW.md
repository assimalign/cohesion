# Scheduler Application Model

Use IApplicationBuilder.AddScheduler to add a build-produced Scheduler manifest to an application graph. Platform gateways consume the resulting platform-neutral ResourcePlan; this assembly is not part of the Scheduler runtime shared framework.
