<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<RootNamespace>Assimalign.Cohesion.Database.Protocol</RootNamespace>
	</PropertyGroup>
	<!--
		A child root of the Database area: the area root (Assimalign.Cohesion.Database)
		references THIS project, never the reverse, so the wire protocol stays an
		independently consumable base component (client tooling can speak the protocol
		without pulling in the area contracts).
	-->
</Project>
