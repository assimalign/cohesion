<Project Sdk="Microsoft.NET.Sdk">
	<!--
		The proven common server core (extracted 2026-07-14, on the second model
		server — the recorded extraction trigger). References only the area root
		and the Connections transport drivers: Protocol, Security, Execution, and
		Types arrive transitively through the root's child-root rollup, and model
		servers derive from the guided base in their own packages.
	-->
	<ItemGroup>
		<CohesionProjectReference Include="Assimalign.Cohesion.Database" />
		<CohesionProjectReference Include="Assimalign.Cohesion.Connections" />
	</ItemGroup>
</Project>
