# Public API

`RezolvrResource` inherits `PlannedResource`; `RezolvrResourceOptions` inherits `ResourceOptions`. `IRezolvrResourceDescriptor` exposes the typed resource and ordinary dependency edges. `AddRezolvr` extends `IApplicationBuilder`; `RezolvrResourceControlPlane.Create` returns an isolated `IResourceControlPlane` with no command kinds.
